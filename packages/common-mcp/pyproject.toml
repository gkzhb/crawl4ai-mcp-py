[project]
name = "common-mcp"
version = "0.1.0"
description = "Common MCP utilities"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "fastmcp>=2.11.3",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"