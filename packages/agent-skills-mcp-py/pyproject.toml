[project]
name = "agent-skills-mcp-py"
version = "0.1.0"
description = "Agent Skills MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.11.3",
    "pydantic>=2.11.7",
    "pyyaml>=6.0.1",
]
